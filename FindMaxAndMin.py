# Find the maximum and minimum elements in a list entered by the user
numbers = list(map(int, input("Enter numbers separated by spaces: ").split()))
print("Maximum number:", max(numbers))
print("Minimum number:", min(numbers))
